query RunInput {
  cart {
    lines {
      id
      quantity
      merchandise {
        ... on ProductVariant {
          id
          product {
            id
            handle
            metafield (namespace: "volume-discount", key:"quantity-based") {
              jsonValue
            }
          }
        }
      }
    }
  }
}
